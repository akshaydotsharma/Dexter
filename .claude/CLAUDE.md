# Dexter

A solo-built productivity stack. Active surfaces today: a native iOS app in SwiftUI and a native macOS app (the `DexterMac` target), both with all data and AI on-device. The web client (`client/`) and Express backend (`server/`) are paused — see "Project status" below.

## Project status: iOS + native macOS, webapp paused

**Don't do new feature work on `client/` or `server/` unless the user explicitly asks.** Webapp development is on hold (decision recorded 2026-05-03). The active clients are the iPhone and the Mac. Data lives in SwiftData on each device; AI calls hit the Anthropic Messages API directly from the app — no Mac server in either client's path.

The **native macOS app** (path 2, NOT Mac Catalyst) shipped via issue #281 / PR #282 (merged to `main` 2026-07-16). All 10 feature sections are ported (Tasks, Today, Lists, Notes, Vocabulary, Activity, Chat, Settings, Finance, Trips); help center is a `ComingSoonView` placeholder. It shares portable source with iOS via a curated source list plus `#if canImport(UIKit)` shims — see "macOS target" below. iPad was explicitly skipped. The Mac runs its OWN local SwiftData store (no CloudKit on free signing); cross-device continuity via the file-based backup restore is the next milestone (not yet built).

If a request would touch the webapp:
- Confirm with the user before doing the work.
- Bug fixes that block the iOS app may proceed (rare — only Dashboard stats / Activity timeline still call the server).
- Don't auto-revive `npm start`, `npm run dev`, or any server smoke flow as part of an iOS task.

## Project memory

Long-lived notes about this codebase live at:
`~/.claude/projects/-Users-akshaysharma-Documents-Projects-Dexter/memory/`

## Agent routing

| User request | Agent | Notes |
|---|---|---|
| iOS feature (SwiftUI view, view-model, service) | `frontend-ux-architect` | Native SwiftUI; `mobile/PersonalDashboard/`. Project file is generated by xcodegen from `mobile/project.yml` — never edit `.xcodeproj` directly |
| iOS App Intent / Shortcuts integration | `frontend-ux-architect` | New intents go under `mobile/PersonalDashboard/Intents/`; register via `DashboardAppShortcuts` |
| iOS AI / Anthropic / draft pipeline change | `frontend-ux-architect` | All under `mobile/PersonalDashboard/AI/`. See "On-device AI" below |
| iOS data layer change (SwiftData @Model) | `frontend-ux-architect` | `mobile/PersonalDashboard/Models/Local/*.swift`. Schema migrations are tricky on existing installs — only ADD fields, don't remove. Shared verbatim with the macOS target |
| macOS feature / porting a new surface to macOS | `frontend-ux-architect` | Native SwiftUI (`DexterMac` target). Add files to the curated `sources:` list in `mobile/project.yml`, shim UIKit with `#if canImport(UIKit)` / rewrite via AppKit. Keep iOS byte-for-byte unchanged. See "macOS target" below |
| New GitHub issue (bug / enhancement / feature) | orchestrator (direct `gh` CLI) | Title MUST follow `.claude/rules/github-issue-conventions.md` — `[Bug]` / `[Enhancement]` / `[Feature]` prefix |
| Product roadmap / PRD / strategy | `product-manager` | |
| Web/server change (paused) | confirm with user first | Don't auto-route to backend-engineer / web frontend-ux-architect without explicit go-ahead |

## Tech stack at a glance (active surfaces)

- **iOS app** (`PersonalDashboard` target): SwiftUI on iOS 17+, SwiftData on-device, `URLSession.bytes` for SSE streaming directly against `https://api.anthropic.com/v1/messages`. `xcodegen` generates the project file. Free Apple personal-team signing (7-day cert expiry).
- **macOS app** (`DexterMac` target): native SwiftUI on macOS 14+ (SwiftData needs 14), NOT Mac Catalyst. `NavigationSplitView` shell instead of the iOS ZStack-router + floating tab bar. Shares portable source with iOS via a curated source list in `project.yml`; UIKit-coupled files are shimmed with `#if canImport(UIKit)`. Own local SwiftData store, own bundle id (`com.akshaysharma.personaldashboard.mac`), `ENABLE_APP_SANDBOX: NO` (dev-only, run from Xcode).
- **AI**: Anthropic `claude-sonnet-4-5`, temperature 0.3, max 1024 tokens. Tool-use loop runs on-device; 15 tools in `mobile/PersonalDashboard/AI/ToolDefinitions.swift`. API key baked into IPA at archive time via `OTA_API_URL` / `ANTHROPIC_API_KEY` build settings.

## Tech stack (paused — leave alone)

- Web client: React 19, Vite 7, Tailwind 4. Dev server :5173.
- Server: Node.js 20+, Express 5, PostgreSQL via `pg`. Default :3000 (falls through to :3001 when squatted). `node --test` against `dexter_test`.
- These still build, but no new features are being added. Dashboard stats + Activity timeline on iOS are still server-bound (need either porting or graceful-degrade — tracked as follow-up).

## Key paths (iOS + macOS)

- `mobile/PersonalDashboard/AI/AnthropicClient.swift` — Messages API client. `send(...)` for one-shot, `stream(...)` for SSE
- `mobile/PersonalDashboard/AI/ChatToDrafts.swift` — capture orchestrator (auto-executes tool calls)
- `mobile/PersonalDashboard/AI/ChatStream.swift` — chat orchestrator (yields drafts, user confirms)
- `mobile/PersonalDashboard/AI/ChatDraft.swift` — UI-facing draft struct + per-action preview text
- `mobile/PersonalDashboard/AI/ExecuteDraftAction.swift` — 15-action SwiftData dispatcher
- `mobile/PersonalDashboard/AI/AssistantContextBuilder.swift` — formats SwiftData state into the EXISTING blocks the LLM needs
- `mobile/PersonalDashboard/AI/ToolDefinitions.swift` — 15 tools with UUID-string ids
- `mobile/PersonalDashboard/Services/CaptureService.swift` — App Intent entry point; wraps `ChatToDrafts` with a 22 s timeout
- `mobile/PersonalDashboard/Services/AIStreamingService.swift` — chat-side wrapper around `ChatStream`
- `mobile/PersonalDashboard/ViewModels/ChatViewModel.swift` — chat state; `confirm(_:)` calls `ExecuteDraftAction`
- `mobile/PersonalDashboard/Models/Local/{LocalTodo,LocalNote,LocalList,LocalNoteFolder}.swift` — SwiftData @Model classes (keyed on `clientUUID`)
- `mobile/PersonalDashboard/Models/Draft.swift` — only `DraftActionType` enum lives here now (other types removed)
- `mobile/PersonalDashboard/Intents/CaptureToDashboardIntent.swift` — Shortcut entry point
- `mobile/PersonalDashboard/Intents/DashboardAppShortcuts.swift` — `AppShortcutsProvider` registry
- `mobile/PersonalDashboard/App/AppConfig.swift` — `apiBaseURL` (legacy, server-bound features only) + `anthropicAPIKey` resolver
- `mobile/ota/ship-lan.sh` — archive + sign + Cloudflare-tunneled IPA + ANTHROPIC_API_KEY injection

### macOS target (`DexterMac`)

- `mobile/PersonalDashboard/App/DexterMacApp.swift` — macOS `@main`. `MacRootView` = `NavigationSplitView` sidebar + `detailView(for:)` switch that wires each ported section; unported sections fall through to `ComingSoonView`. MUST be in the iOS target's `excludes` or you get two `@main`
- `mobile/PersonalDashboard/InfoMac.plist` — macOS Info.plist (also in iOS `excludes`)
- `mobile/PersonalDashboard/Design/PlatformImage.swift` — `PlatformImage` = UIImage/NSImage alias, `Image(platformImage:)`, `jpegDataCompat`/`cgImageCompat`
- `mobile/PersonalDashboard/Design/PlatformModifiers.swift` — iOS-only view-modifier shims (`noAutocapitalization`, `urlKeyboard`, `decimalKeyboard`, `inlineNavigationTitle`, `listSectionSpacingCompat`, `ToolbarItemPlacement.trailingBar`); each is a no-op on macOS so shared views keep one fluent chain
- `mobile/PersonalDashboard/Views/Trips/TicketViewers.swift` — cross-platform ticket/PDF/zoom viewers relocated out of the camera-gated `TicketScanView`
- `mobile/PersonalDashboard/Views/Shell/BottomTabBarMetrics.swift` — UIKit-free metric extracted so ported views don't drag in the iOS-only tab bar
- `mobile/project.yml` — the `DexterMac` target's **curated `sources:` list** is the source of truth for what's ported. Adding a macOS surface = add its transitive files here, then `xcodegen generate`

## On-device AI

The capture (Shortcut) and chat surfaces both run entirely on the device:
- `CaptureService` → `ChatToDrafts.run()` → `AnthropicClient.send()` (with 15 tools) → `ExecuteDraftAction.run()` (mutates SwiftData) → dialog summary back to App Intent.
- `ChatViewModel` → `AIStreamingService.parseStream()` → `ChatStream.run()` → `AnthropicClient.stream()` → text deltas + `ChatDraft` cards. User taps Confirm → `ExecuteDraftAction.run()` on SwiftData.

The same 15 tools serve both paths — capture auto-executes, chat surfaces drafts for review. Tool input ids are UUID strings (the SwiftData `clientUUID`), not integers.

## Project rules

Forward-looking rules live in `.claude/rules/`. Read every file in that directory before doing project work.

- `.claude/rules/github-issue-conventions.md` — issue title format and label conventions
- `.claude/rules/dev-workflow.md` — local-first iOS workflow, tests-before-commit, commits link to issues

## Gotchas

- `mobile/PersonalDashboard.xcodeproj/` is gitignored — always regenerate via `xcodegen generate` from `mobile/project.yml` before building. SourceKit may show stale "cannot find" errors for new files until you regenerate.
- iOS free personal-team profile expires every 7 days. Re-run `bash mobile/ota/ship-lan.sh` to ship a fresh build.
- **CloudKit / iCloud KV / iCloud Documents are NOT available on free personal-team signing.** They require the paid Apple Developer Program. Until that upgrade, the iOS app is single-device only — Apple's automatic device backup covers SwiftData restore on a new phone but there's no real-time multi-device sync.
- **Anthropic API key lives in the IPA Info.plist.** Extractable with `strings`. Acceptable for a personal dev-signed app you don't redistribute. Set a low monthly spend cap on the key in the Anthropic console as defense-in-depth.
- **iOS Local Network permission masquerades as DNS error.** If the iOS app shows *"A server with the specified hostname could not be found"* on a private IP (LAN, Tailscale 100.x.x.x, RFC1918), it's almost always Local Network permission, NOT DNS. Fix is the `LocalNetworkPermissionPrimer` (NWBrowser primer) called from `PersonalDashboardApp.task`, plus `NSBonjourServices: [_http._tcp]` and `NSLocalNetworkUsageDescription` in Info.plist. (Less relevant now that Anthropic is the primary endpoint, but the dashboard stats / activity timeline still hit the Mac.)
- SwiftData @Model migrations are risky. Adding fields with defaults is safe. Removing fields triggers a lightweight migration; if it fails, the user loses everything. Prefer to leave dead fields in place rather than remove them. (`needsSync`, `version` on the local models are intentional dead fields kept for migration safety.)

### macOS target gotchas

- **The `DexterMac` target uses a CURATED source list, not whole-dir globs.** A new shared file compiles on iOS (globbed) but is invisible to macOS until added to `DexterMac`'s `sources:` in `project.yml`. Missing-file build errors on the Mac target almost always mean "add it to the list + `xcodegen generate`".
- **Two `@main` trap.** The iOS target globs the whole dir, so `App/DexterMacApp.swift` and `InfoMac.plist` are in the iOS target's `excludes`. Don't remove them.
- **Keep iOS byte-for-byte unchanged when porting.** Shim shared UIKit-coupled files with `#if canImport(UIKit)` (iOS body) / `#elseif canImport(AppKit)` (macOS body); use `#if os(iOS)` for phone-only SwiftUI modifiers/views. Never delete the iOS path.
- **No CloudKit on free signing → the Mac has its own separate local SwiftData store.** Same 15-model schema, different DB file. There is no live sync yet; the file-based backup restore is the planned bridge.
- **macOS SwiftUI ignores synthetic accessibility clicks** on tap-gesture / List-selection controls, so autonomous QA can drive create-flow Buttons but NOT complete/edit/delete tap rows or swipe-delete. Those need a hands-on pass; build-verified ≠ QA'd (same framing as iOS — see project memory `feedback_qa_framing.md`).
- **Build both targets after any shared-file change.** SourceKit "cannot find X" for cross-file refs is STALE/false; only `xcodebuild` is authoritative.

## Dev workflow

```bash
cd mobile
xcodegen generate                            # regenerate .xcodeproj from project.yml (both targets)

# --- iOS (device path) ---
open PersonalDashboard.xcodeproj             # simulator / device runs from Xcode
bash ota/ship-lan.sh                         # archive + sign; produces /tmp/ota/app.ipa (needs ANTHROPIC_API_KEY, sourced from server/.env)
xcrun devicectl device install app \
    --device <UDID-from-`xcrun devicectl list devices`> \
    /tmp/ota/app.ipa

# --- macOS (build + run) ---
xcodebuild -project PersonalDashboard.xcodeproj -scheme DexterMac \
    -destination 'platform=macOS' -configuration Debug build
open PersonalDashboard.xcodeproj             # run DexterMac scheme from Xcode

# --- verify no iOS regression after a shared-file change ---
xcodebuild -project PersonalDashboard.xcodeproj -scheme PersonalDashboard \
    -destination 'generic/platform=iOS' build
```

The web client and server still build (`npm run install:all`, `npm start`) but new feature work on them is paused — confirm with the user before touching.

## Deployment

- iOS: free dev builds shipped via OTA + `devicectl device install` over wifi. No App Store distribution today.
- macOS: dev-signed, run from Xcode (`ENABLE_APP_SANDBOX: NO`). No packaged/distributed build yet — revisit sandbox + signing before any distribution and before the iCloud-backup restore milestone.
- Web/server: previously deployed to Railway (`railway.json`). Currently paused. The startCommand reference in `railway.json` is broken; if/when web revives, fix it before redeploying.
